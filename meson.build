project('LibMVC-all', 'cpp',
  default_options: [
    'cpp_std=c++14',
    'buildtype=release'])

warning('MESON builds are experimental')

subdir('LibNuMVC')
subdir('LibFastVC')
subdir('ParallelSolverAdapter')
subdir('test')
subdir('bench')

